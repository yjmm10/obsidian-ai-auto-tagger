# 变更日志

本文件记录每个版本的**功能点**与**改进**，供 GitHub Release 自动提取展示。
发版流程：更新本文件对应版本段 → `npm version <patch|minor|major>` → `git push && git push --tags`。

## [1.7.0] - 2026-07-14

### 捐赠 / 支持功能升级

- **多平台捐赠入口**：「关于」页新增「☕ 支持作者」卡片，除原有微信收款码外，新增 Buy Me a Coffee、GitHub Sponsors、爱发电、PayPal、支付宝 五个可配置平台入口。
- **链接可配置**：各平台捐赠链接在设置页直接填写（留空则不显示），仅保存在用户本地设置，无需硬编码；关于页根据所填链接实时生成可点击的捐赠按钮。
- **微信收款码保留**：原有微信支付收款码继续展示，作为「支持作者」卡片的默认入口之一。
- **官方社区插件捐赠入口**：`manifest.json` 新增 `fundingUrl` 字段（对象，多平台：Buy Me a Coffee / GitHub Sponsors / 爱发电 / PayPal）。提交到 Obsidian 社区目录后，插件详情页会自动显示「赞助」按钮，与设置页「支持作者」卡片互补；微信收款码为图片形式，仅保留在设置页（fundingUrl 仅接受 URL）。

## [1.6.5] - 2026-07-14

### 设置页精简与自适应布局

- **精简说明文字**：移除大量冗余的设置项说明（厂商、模型、字段名、类型、描述、文件夹列表等），保留的关键说明压成短语，功能说明言简意赅。
- **去掉字段卡片头部的"描述预览"行**（即标签下的说明），减少视觉拥挤。
- **自适应布局**：字段名 + 类型并排（多列）；生成模式 / 标签来源 / 标签文件路径 / 描述占整行，描述与路径为多行文本框，长内容不挤压、不溢出。
- 收紧字段网格内边距，新增 `min-width:0` 防止窄列溢出。

## [1.6.4] - 2026-07-14

### 移除「允许取值」设置字段

- 设置页字段卡片中彻底移除「允许取值」（constraints）输入项——对所有字段类型（含 数值 / 布尔）均不再显示，分类类字段本就由「标签来源」覆盖，数值/布尔字段现直接由 AI 自由生成。
- 同步清理相关提示：数值/布尔字段的预定义提示不再引导至「允许取值」。

## [1.6.3] - 2026-07-14

### 字段卡片布局与约束精简
- **「允许取值」按类型条件显示**：分类类字段（array / string）已被「标签来源」覆盖，不再显示「允许取值」整行；仅 数值 / 布尔 字段保留「允许取值」作为唯一取值约束手段（无标签来源），提示文案同步改为说明此范围。
- **标签文件路径改为两行文本框**：`tagFilePath` 输入框从单行改为两行 `textarea`，完整显示长路径，并支持纵向拉伸；Enter 始终选定候选 / 提交（不再误插入换行）。
- **「说明」→「描述」并移到卡片开头**：字段卡片的字段说明项改名为「描述」，并调整到卡片最顶部（字段名 / 类型 / 生成模式 之前）。

## [1.6.2] - 2026-07-14

### 设置页交互改进
- **生成模式占整行**：字段卡片的「生成模式」（直接生成 / 使用预定标签 / 混合）下拉现在独占一整行，更醒目、更易点击，避免与类型选择挤在同一行。
- **所有路径输入支持智能联想**：标签文件路径（`tagFilePath`）输入框现在与文件夹路径输入框一样，输入时自动联想库内文件，点击或回车即可选定；联想组件抽为通用 `attachPathSuggest`，文件夹与文件路径输入共用同一套体验（候选过滤、↑/↓ 键盘导航、子串高亮、斜杠折叠匹配 `03/01` ↔ `0301`、点击选中）。
- **提示文案**：标签文件路径说明补充「输入时自动联想库内文件」提示（中/英）。

## [1.6.0] - 2026-07-13

### 核心改进：自动触发实时化 🚀
- **空字段 / 被删除字段始终实时补全**：无论「已有标签处理策略」如何设置，只要某个字段缺失或被删除（如清空了 `tags`），下一次触发都会立即重新生成并写回，不再被字数门槛阻挡。彻底解决「没有标签 / 删掉标签却迟迟不更新」的问题。
- **字数门槛只拦「全满」场景**：仅当「所有字段都已存在且正文不足最小字数」时才暂缓调用、挂起等待后续写入达标；只要有待补全的空字段，短笔记也会即时打标。
- **防抖更跟手**：默认防抖由 3000ms 降至 2000ms，停止编辑约 2 秒即发起 AI 调用。
- **修复自触发循环隐患**：把原先「写回期间忽略事件」的脆弱标记改为「写回后宽限窗口」机制，避免插件自身写回 frontmatter 时误触发二次打标或漏更新，保证实时性稳定可靠。

### 默认策略调整
- 默认「已有标签处理策略」由 `保护（跳过）` 改为 `合并`：保留你已喜欢的标签，同时 AI 实时补充新标签/字段，兼顾「实时更新」与「不破坏已有成果」。空字段/被删除字段仍始终补全。
- 字段级策略改为**逐字段**判断：先前整篇有标签就跳过（导致空字段也永远不被填），现在每个字段独立决策，缺失字段必被补全。

### 文档与提示
- 设置页「触发与行为」新增醒目的 **⚡ 实时性** 提示框；「触发说明」「已有标签处理策略」「最小正文字数」文案同步更新，明确实时补全规则。

### 工程
- 将 `applyFields` / `isEmptyValue` 等纯逻辑抽到不依赖 Obsidian 的 `src/field-apply.ts`，便于单元测试覆盖实时补全行为（新增 14 个用例，测试 82/0 通过）。

## [1.5.0] - 2026-07-13

### 新增
- **预定义标签改为字段级**：「使用预定标签 / 混合」模式的标签来源（标签文件 / 自动检索库 / 两者）与标签文件路径，现在**每个字段独立配置**，不再使用全局统一设置；不同字段可使用完全不同的标签池，互不干扰。设置项随字段「生成模式」自动出现/隐藏。
- **插件品牌标识**：设置头部与「关于」页面新增渐变 Logo（AI + 标签 + 闪光），「关于」页新增品牌区（Logo + 名称 + 标语），界面更有辨识度。

### 改进
- **字段卡片视觉重构**：卡片头部加粗字段名、胶囊式类型/模式徽章；预定义/混合模式卡片带左侧强调条与淡色渐变背景；卡片底部新增字段说明预览行（折叠态也可见）；悬停浮起阴影。
- **递归与预定义解耦**：递归生效范围 / 排除文件夹属于「生效范围」配置（独立），与字段级预定义标签完全分离，概念更清晰。
- **兼容迁移**：旧版（≤1.4.0）保存在全局的 `tagSource` / `tagFilePath` 会在加载时自动迁移到各字段，已有配置不丢失。

## [1.4.0] - 2026-07-13

### 新增
- **关于页面**：设置页新增「关于」标签页，展示插件版本、作者信息与开发者寄语，并内嵌微信支付收款码二维码，方便支持作者。
- **字段主开关**：「提取字段」卡片头部新增「启用全部字段」主开关，一键启用/禁用所有字段；每个字段卡片头部也新增了独立的启用开关，折叠时即可操作。
- **系统提示词配置**：将原来的「自定义 system 提示前缀」升级为完整的「系统提示词」配置项，用户可自定义 system prompt，并支持一键恢复默认；旧版 `extraInstruction` 会自动迁移。

### 改进
- **最大输出 token 语义明确化**：设置页新增说明，强调「最大输出 token」仅限制 AI 生成的字段 JSON 长度，不包含系统提示词、标题与正文。
- **字段卡片折叠优化**：字段配置默认折叠，点击卡片头展开；卡片头显示字段名、类型、生成模式徽章，并支持整头点击折叠/展开。
- **字体与 emoji 优化**：整体字号缩小一档，界面更紧凑；为头部标签、设置项、按钮、卡片标题等位置加入丰富的 emoji 图标，提升可读性与氛围。
- **吸顶 header 层级修复**：提升吸顶头部的 `z-index`，避免目录联想下拉等内容在滚动时穿透标签导航。

## [1.3.0] - 2026-07-13

### 新增
- **字段级「生成模式」**：每个字段现在支持三种取值来源——`直接生成`（AI 自由产出）、`使用预定标签`（仅从预定义标签池中选择，绝不引入池外新值）、`混合`（AI 生成 + 始终并入预定义标签池）。对应设置项 `FieldMapping.mode`。
- **预定义标签池**：新增「预定义标签池」配置卡片，可指定标签来源——`标签文件`（读取库内指定文件，每行一个标签或 YAML `tags:` 列表）、`自动检索库`（扫描库内所有笔记 frontmatter 的 `tags`）、`两者`（并集，默认）。为字段的「使用预定标签 / 混合」模式提供词表。
- **触发条件说明与可控**：设置页「触发与行为」区新增提示框，明确「新建 / 网页剪藏」「保存 / 更新」「手动命令」的对应关系与默认行为（默认自动触发，正文满 `minContentChars` 字自动更新，不足阈值仅可手动触发）。

### 改进
- **已有标签策略保留**：「保留现有 / 现有 + 新生成 / 新生成」三策略（`tagPolicy`）继续作为全局保护开关与字段级 `mode` 协同工作——`skip` 在有标签时整篇跳过，绝不改动你喜欢的标签。

## [1.2.0] - 2026-07-13

### 新增
- **字段卡片可折叠**：每个「提取字段」卡片头部新增折叠箭头，点击展开 / 收起该字段的全部控件，字段较多时面板更清爽、更易管理。
- **中英文界面**：新增语言切换（头部右上角「中 / EN」），一键在中文与英文之间切换整个设置界面；厂商说明同步提供双语。设置以 `locale` 字段持久化，默认中文。

### 改进
- **恢复配置整合**：将原分散的「恢复默认字段」与「恢复全部默认配置」统一收进设置页底部的「恢复配置」卡片，两处重置入口集中、清晰可见。
- **顶部标签与内容滚动优化**：设置面板改为「头部固定 + 内容区内部滚动」布局——标签页始终钉在顶端，正文在独立区域内平滑滚动，不再与头部相互干扰；并新增滚动条样式与语言切换控件样式。

## [1.1.0] - 2026-07-13

### 新增
- **生效范围递归开关**：设置页新增「包含子文件夹（递归）」，可控制生效文件夹是否向下包含任意层级的子文件夹；关闭时仅处理该文件夹内的直接文件。
- **已有标签处理策略**：将原先「覆盖字段 / 已有标签跳过」两个布尔开关，整合为清晰的三选一策略：
  - `保护已有（有标签则跳过）`（默认）：笔记已有非空 `tags` 时整篇跳过，绝不改动你喜欢的标签。
  - `合并`：保留已有值，AI 仅补充新标签 / 字段（数组去重追加，标量仅当原值为空时写入）。
  - `覆盖`：AI 全权重写所有目标字段。

### 改进
- **发布可追溯**：引入 `CHANGELOG.md`，GitHub Release 自动提取本文件对应版本段作为发布说明，结构化呈现功能点与改进内容。
- 设置页「已有标签处理策略」默认保持「保护」，确保升级后你既有的标签不会被误覆盖或丢失。

## [1.0.0] - 2026-07-13

### 新增
- 多厂商 AI 接入：OpenAI / Anthropic / Google 及兼容 OpenAI 协议的国内厂商（智谱、DeepSeek 等）与 Ollama 本地模型。
- 连接验证：设置页「测试连接」按钮，返回原始响应便于排查。
- 字段映射：自定义 frontmatter 字段（标签 / 摘要 / 分类等），支持「允许取值」约束（提示词约束 + 数组回落过滤）。
- 多文件夹生效范围，支持输入时目录联想（如输入 `0301` 实时提示匹配目录）。
- 触发方式：新建文件（含网页剪藏）/ 内容新增 / 手动命令 / 批量处理文件夹。
- 内容达标门控：正文不足阈值时挂起，待写够后自动触发，避免对空文件浪费 AI 调用。
- 易用设置页：吸顶双标签页（AI 打标签 / AI 配置）、字段卡片、目录联想、三类重置（重置参数 / 恢复默认字段 / 恢复全部默认）。
- 默认参数：最大输出 100 token、送入 AI 最大 1000 字、触发达标最小 300 字、批量并发 5。
